# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'integration with googlemaos' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for integration with googlemaos

  target 'integration with googlemaosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'integration with googlemaosUITests' do
    # Pods for testing
  end

 pod 'GoogleMaps'
  pod 'GooglePlaces'
pod 'Alamofire'


pod 'SwiftyJSON'

end
